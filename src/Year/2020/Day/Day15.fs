module Year2020.Day15

open AdventOfCode

let rambunctiousRecitation (input: string) =
    //let nums = [1,1;3,2;2,3] |> Map.ofList
    //let _,a = seq { 4 .. 2020 }
    //            |> Seq.fold (fun (mo,mr,p) i -> 
    //                            match mr |> Map.containsKey (p) with
    //                            | true -> (mr, (Map.add i (mr.[p] - mo.[p]) mr), i)
    //                            | false -> (mr, (Map.add i 0 mr), i)) (nums,nums,3)
    
    Answer.NoAnswers

